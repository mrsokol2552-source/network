# === ФАЙЛ ПРИМЕРА .env ========================================================
# Скопируй этот файл в .env и заполни значения.
# Скрипт читает их как переменные окружения (если они заданы),
# иначе спросит логин/пароль интерактивно.
#
# ВАЖНО:
#  - Никогда не коммить .env в общий репозиторий.
#  - Для разных площадок можно иметь несколько .env.<site> и переименовывать.
# ==============================================================================

# Базовые креды для устройств (если у всех одинаковые)
NET_USER=zhiltsov_a
NET_PASS=Sasha255255sasha
# Enable secret (для Cisco IOS, если требуется)
NET_ENABLE=
# Максимум параллельных потоков при сборе
CONNECT_TIMEOUT=3
AUTH_TIMEOUT=12
TEXTFSM_WORKERS=8
SENDCMD_TIMEOUT=30
HOST_DEADLINE=120