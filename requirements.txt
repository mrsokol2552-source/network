# === ЗАВИСИМОСТИ ДЛЯ ЭТАПА 1 (и на будущее для Этапов 2–3) =====================
# Устанавливаются одной командой:
#   pip install -r requirements.txt
#
# ПРИМЕЧАНИЯ:
# - Версии указаны «не ниже», чтобы избежать багов старых релизов.
# - Если окружение корпоративное (прокси/эрразинг), прокинь параметры pip:
#     pip --proxy=http://user:pass@proxy.host:3128 install -r requirements.txt
# - На Windows при проблемах с paramiko может понадобиться:
#     pip install cryptography>=42
# ===============================================================================
netmiko>=4.3
paramiko>=3.4
rich>=13.7
tqdm>=4.66

# Для будущего Этапа 2 (парсинг) — можно поставить заранее
textfsm>=1.1.3
ntc-templates>=3.7.0

# Для удобной работы с YAML/JSON и IP
pyyaml>=6.0.2